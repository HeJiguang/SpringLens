<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.6</version>
        <relativePath/>
    </parent>

    <groupId>io.springlens</groupId>
    <artifactId>spring-lens</artifactId>
    <version>0.1.0</version>
    <packaging>pom</packaging>

    <name>Spring Lens</name>
    <description>AI Runtime Operating System for Spring Boot applications.</description>

    <modules>
        <module>spring-lens-model</module>
        <module>spring-lens-spi</module>
        <module>spring-lens-runtime</module>
        <module>spring-lens-starter</module>
        <module>spring-lens-agent-contract</module>
        <module>spring-lens-agent-starter</module>
        <module>spring-lens-server</module>
        <module>spring-lens-demo-app</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <spring-ai.version>1.1.3</spring-ai.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <configuration>
                        <useModulePath>false</useModulePath>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
